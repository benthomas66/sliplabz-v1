# V1 open gaps register

**Governance instrument.** One row per known structural gap surfaced by a ticket. Every row is traceable to a specific report + section. Status reflects the record at commit time (2026-07-17); "blocks launch" is a factual read against the current V1 goal, not a triage priority.

Do NOT extend this register with speculation or wishlist items. Add a row only when a ticket report has surfaced the gap with citation.

| id | name | where found | current status | assigned | blocks launch? |
|---|---|---|---|---|---|
| GAP-1 | `real_line_windows` has no initial-population driver | V1-4c Phase A report §"Definitive answer" (line 28) and §D.4 Phase B recommendation | **OPEN.** V1-5 recomputation writer handles CORRECTIONS only (`recomputationWriter.ts:1`); no committed driver performs first-pass population. `real_line_windows` remains empty on hosted. | UNASSIGNED — Phase A explicitly deferred to a separate ticket | **No.** The engine consumes THRESHOLD windows via `computeThresholdWindow` per §A.1; it does not read `real_line_windows`. |
| GAP-2 | Forward game ingestion — `games` stopped at 2026-07-12 | V1-4d report §Executive Summary + §STEP 3 (line 13: "The `games` table stops at 2026-07-12"); V1-4e report §"Executive summary" (line 10: range extended to 2026-09-25) | **CLOSED by V1-4e** (158 new games created; watermark advanced; forward season loaded through 2026-09-25). Verified by V1-4e §Hosted proof (a). | V1-4e | n/a — closed |
| GAP-3 | Seed + stats backfills are one-shots — evidence windows drift stale as the season runs | V1-4e report §"Stats + closing-line gap (recovered 07-13..07-15 games)" (line 91-93) and §Executive summary bullet on 2026-07-13..07-15 recovered games | **OPEN.** No `historical_line_results` or `player_game_stats` exist for the recovered 2026-07-13..07-15 games. As the season progresses the drift compounds; seed and stats backfills need periodic top-up. Ticket-forbidden ingestion in V1-4e. | UNASSIGNED — periodic operations owner | **Yes for correctness.** Missing recent-game stats bias evidence windows for players in those games. Ranks below the freshness ruling in blocking severity but must land before launch. |
| GAP-4 | No sanctioned `event_reconciliation_queue` drain writer | V1-4e report §STEP 1(b) (line 27-34: "NO drain writer exists"); §STEP 3 queue-row hygiene note (line 113); §Hosted proof (b) (line 320) | **OPEN.** All 5 V1-4e-target queue rows remain `resolution='open'` even after their `provider_games` were approved. `persistSeedEventResolution` never updates the queue. `grep "UPDATE event_reconciliation_queue" src/` returns nothing; `grep "resolution\s*=\s*'resolved'" src/` returns nothing. | UNASSIGNED | **No.** Cosmetic (queue rows leaking as `open` after resolution) — does not affect grain visibility or profile output. |
| GAP-5 | No sanctioned back-link path for `market_snapshots.linked_internal_game_id` | V1-4e report §STEP 1(a) (line 24-25); §STEP 4 (line 117); §Hosted proof (c) (line 325) | **OPEN.** Column is populated only at INSERT by `persistOddsapiSnapshot` from caller-supplied input; no committed code path updates it after approval. 84 V1-4d snapshots remain NULL-linked; V1-4e's bounded re-poll wrote fresh linked snapshots instead. | UNASSIGNED | **No.** V1-4e re-poll pattern replaces the need; but the durable fix (backfill on approval) is desirable when a poll is expensive. |
| GAP-6 | `game_status_observations` is empty; status transitions have no sanctioned owner | V1-4e report §"Status-pipeline conflict I was asked to flag" (line 86-89) | **OPEN.** V1-4b and V1-4e both write `games.status` directly (INSERT/UPDATE only, no observations row). BDL post-final updates do not drive `games.status`. Systemic gap flagged by V1-4e and belongs to a separate ticket. | UNASSIGNED | **No** for now — V1 launch does not depend on status transition auditability, but this must be addressed before any surface reads `game_status_observations` for change history. |
| GAP-7 | Two freshness classifiers under one enum measure different clocks and different terminal states | V1-4e freshness review §Q1-Q2 (this doc: `docs/product/reports/V1_TICKET_4E_FRESHNESS_REVIEW.md`); confirmed by V1-4f report §Executive summary and §"Why two classifiers" | **OPEN.** `src/odds/freshness.ts` measures `now − provider_last_update` (600/1800, terminal `stale`); `src/computation/freshness.ts` measures `now − MAX(observed_at)` (90/300/900, terminal `unavailable`). Same `FreshnessState` enum. The engine consumes only the second via `CurrentMarketRow`. Owner ruled 2026-07-16 that these are two distinct metrics, not conflicting versions of one metric. | **assigned to tickets D and E** (per owner ruling 2026-07-16) | **Yes.** Any change to what the engine consumes requires a new `method_version` per DR-24 + regression fixtures per A1 §12; must land before launch. |
| GAP-8 | §C.3 and §15.2 conflict; §15.2 "wins in code," making §C.3's stale-cap branch structurally unreachable | V1-4e freshness review §Q6-Q7 (line 91-127: "structurally unreachable in the current read-model pipeline"); V1-4f report §Tensions #2 (line 266) | **OPEN.** Composer's `isFreshEnoughForConsensus` (`currentMarketRow.ts:60-63`) empties the offering set for state ∉ {fresh, aging}; every stale grain therefore arrives at the engine with `book_count = 0`; `evaluateC3Freshness('stale', 0)` → `no_current_market_unavailable`, never `stale_current_market_cap`. **Owner ruled: §C.3 wins for evidence consumption**; the code needs to be brought into line. | **assigned to ticket D** (per owner ruling 2026-07-16) | **Yes.** Directly changes what profiles the engine emits; blocks launch until resolved. |
| GAP-9 | Poll wall-clock (299 s) vs a 90 s fresh window; requests are sequential | V1-4f report §Executive summary (line 11) + §Measurement B1 (line 62) + §Tensions #1 (line 265) | **OPEN.** Under sequential per-event polling of 5 events × 4-5 books × 4 markets, poll persist takes ~5 min. The composer's 90 s `FRESH` window cannot be entered by the majority of grains; only the LAST event's grains are ever fresh at engine time. Direct architectural interaction of poll cadence + serialization + threshold magnitude. | **assigned to ticket B** (polling optimization) | **Yes for the operational product.** Even if GAP-7/GAP-8 rulings loosen the composer window, the ~5 min poll dominates cadence choice. |
| GAP-10 | §I.3 assigns the first-profile record to "the V1-A1-3 ticket report"; V1-4e actually discharged it | V1-4e report §"Assigned-report caveat" (line 304); mirror caveat in the freshness review | **OPEN.** Documentation-only. §I.3's wording should be aligned to "the ticket that persists the first operative profile" rather than naming V1-A1-3 specifically. The record's SUBSTANCE (five §I.3 fields) is complete on V1-4e. | UNASSIGNED — documentation alignment | **No.** Substance intact. |
| GAP-11 | Five compact cap tags live in BOTH authority §D.4 and `compose.ts:cap_summary_short` with nothing asserting they match | Cross-file: `docs/product/EVIDENCE_PROFILE_METHOD_V1.md` §D.4 (line 464, "Ratified compact cap tags (OWNER-RATIFIED 2026-07-16, v1.3)"); `src/explanation/compose.ts:149` writes `cap_summary_short`; `tests/explanation/authorityConformance.test.ts` exists but does not currently assert the pair | **OPEN.** No test asserts the code's five tag strings match the authority's five ratified tags. A silent drift is possible on any future edit of either side. Extend `tests/explanation/authorityConformance.test.ts` to enforce the pair. | UNASSIGNED — extend the existing authority-conformance test | **No.** Regression risk only; no current divergence. |
| GAP-12 | v2 stale-present branch may be unreachable in production via the v1 composer offering-set gate | V1-A2-4 report (closing note), 2026-07-24 | **OPEN — under governor review; blocks V1-A2-3 live validation.** The read-model composer empties the offering set for grains outside v1's fresh/aging window (`src/computation/currentMarketRow.ts`, gate around lines 60-63, using `src/computation/freshness.ts` thresholds). Because the v2 populator consumes a `CurrentMarketRow` produced by that composer, a grain older than the v1 gate arrives at the v2 engine with `eligible_book_count = 0` and is classified `absent` (Unavailable + NO_CURRENT_MARKET) rather than reaching v2's `stale-present` branch (Moderate cap + STALE_CURRENT_MARKET). This is the same structural unreachability that motivated evidence_method_v2, relocated upstream of the engine. V1-A2-2's REVISE fixed the ENGINE's freshness verdict; the COMPOSER gate is a different point in the pipeline and was not addressed (`currentMarketRowV2.ts` was deleted during the REVISE under repair 8 as having zero callers — true at the time, because no production wiring existed). Closely related to GAP-8 (same composer gate, v1 side). **RESOLVED by V1-A2-5 (2026-07-24)** — the composer was split into a freshness-neutral assembly core (`src/computation/currentMarketRowCore.ts`), a v1 wrapper retaining the wall-clock gate, and a v2 wrapper (`src/evidence/v2/freshnessNeutralMarketRow.ts`) applying no gate and no clock. A grain older than v1's ~300s window now remains structurally market-present for v2; disposition is decided solely by `classification_age` against the D-A1 thresholds. | V1-A2-5 | **No** (was: yes). |
| GAP-13 | V1-6a preview deployment pending Vercel provisioning | V1_TICKET_6A_REPORT.md Scope I, 2026-07-24 | **OPEN — pending external provisioning by the founder.** Scopes A-H of V1-6a were validated locally. Scope I (Vercel PREVIEW deployment) could not run — no Vercel project or credentials were available to the agent, and creating them is a founder action. Consequence: the amended V1-6 design review has no deployed artifact to review, and per founder amendment 11 a successful preview deploy additionally requires RE-RUNNING the serialization audit against the DEPLOYED response, since local results are not evidence for the deployed artifact. **RESOLVED by V1-6c (2026-07-25).** Preview deployed from the repo root (Root Directory `apps/web`) at `https://web-d6bk2hzqa-bens-projects-593972b9.vercel.app`; the committed `deployedResponse.test.ts` passed 2/2 against the LIVE preview (no prohibited values / secrets in the raw HTML incl. `<script>`; `__next_f` flight marker present; positive control = the approved EMPTY STATE, hosted holding zero v2 profiles; client bundles clean). Required a scoped fix (`outputFileTracingRoot` → repo root, V1-6c) and two founder settings fixes (Output Directory → Next.js default; Deployment Protection off for previews). | V1-6c | **No** (design review now has a deployed artifact). |
| GAP-14 | Board service imports the shared library by deep relative path | V1-6a governor review, 2026-07-24 | **OPEN — cosmetic/maintainability.** `apps/web`'s board service imports the committed DR-20 comparator from the root library via a five-level relative traversal (`../../../../../src/evidence/classification.js`). The dependency direction is CORRECT — the app should consume the shared library rather than duplicate a comparator — but the path is brittle to any future directory move on either side. Resolve with a path alias in `apps/web`'s tsconfig the next time that file is legitimately opened. Not a defect; no behaviour is at risk today. | UNASSIGNED | **No.** |
| GAP-15 | apps/web is pinned to the deprecated-path webpack builder | first Vercel deployment attempt, 2026-07-25 (build log: Turbopack default warning, then WorkerError) | **OPEN — deliberate deferral.** Next 16 defaults to Turbopack. `apps/web` pins `--webpack` because (a) `next.config.mjs`'s extensionAlias (`.js`->`.ts`) is the mechanism by which the app consumes committed backend modules (dr20Compare, the compact renderer) without duplicating them, and (b) the committed serialization audit's bundle scan must run against the same builder that produces the deployed artifact. Migration to Turbopack requires: an equivalent module-resolution mechanism verified under Turbopack; every `next build` invocation switched together; and the FULL serialization audit re-run under the new builder as the acceptance standard. Note: webpack support in Next.js is on a deprecation path; this pin has a shelf life measured in major versions, which is why the gap exists rather than the pin being permanent. | UNASSIGNED (future ticket) | **No.** |

---

## Traceability index

Every row above cites a specific report + section. Reports live in `docs/product/reports/`:

- `V1_TICKET_4C_REPORT.md` — Phase A historical-line-results survey (GAP-1)
- `V1_TICKET_4D_REPORT.md` — first live current_poll (GAP-2 origin)
- `V1_TICKET_4E_REPORT.md` — forward game ingestion; back-link + queue-drain audits (GAP-2 close, GAP-3, GAP-4, GAP-5, GAP-6, GAP-10)
- `V1_TICKET_4E_FRESHNESS_REVIEW.md` — targeted governor review answering 12 questions (GAP-7, GAP-8)
- `V1_TICKET_4F_REPORT.md` — freshness decay & book movement probe (GAP-9)
- `EVIDENCE_PROFILE_METHOD_V1.md` §D.4 + `src/explanation/compose.ts` — cap-tag pair (GAP-11)
- `V1_TICKET_A2_4_REPORT.md` — v2 read-model input builder; composer-gate closing note (GAP-12)
- `V1_TICKET_A2_5_REPORT.md` — freshness-neutral composer core + v1/v2 wrappers (GAP-12 resolution)
- `V1_TICKET_6A_REPORT.md` — Board vertical slice; server-only boundary + committed serialization audit (GAP-13, GAP-14)
- `V1_TICKET_6B_REPORT.md` — builder parity (webpack pin) + preview deployment (GAP-13 close, GAP-15)
- `V1_TICKET_6C_REPORT.md` — preview deploy + deployed-response audit against the live URL (GAP-13 resolution)

## Register maintenance

- A gap moves to **CLOSED** only when a ticket report demonstrates the fix + verification.
- A gap moves to **DEFERRED** only by explicit owner ruling with the ruling cited.
- Do NOT delete rows — closed rows remain here as governance history.
- "Blocks launch?" is a binary factual read against the current V1 launch definition; adjust when the launch definition changes, cite the change.
