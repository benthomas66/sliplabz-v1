# V1 open gaps register

**Governance instrument.** One row per known structural gap surfaced by a ticket. Every row is traceable to a specific report + section. Status reflects the record at commit time (2026-07-17); "blocks launch" is a factual read against the current V1 goal, not a triage priority.

Do NOT extend this register with speculation or wishlist items. Add a row only when a ticket report has surfaced the gap with citation.

| id | name | where found | current status | assigned | blocks launch? |
|---|---|---|---|---|---|
| GAP-1 | `real_line_windows` has no initial-population driver | V1-4c Phase A report §"Definitive answer" (line 28) and §D.4 Phase B recommendation | **OPEN.** V1-5 recomputation writer handles CORRECTIONS only (`recomputationWriter.ts:1`); no committed driver performs first-pass population. `real_line_windows` remains empty on hosted. | UNASSIGNED — Phase A explicitly deferred to a separate ticket | **No.** The engine consumes THRESHOLD windows via `computeThresholdWindow` per §A.1; it does not read `real_line_windows`. |
| GAP-2 | Forward game ingestion — `games` stopped at 2026-07-12 | V1-4d report §Executive Summary + §STEP 3 (line 13: "The `games` table stops at 2026-07-12"); V1-4e report §"Executive summary" (line 10: range extended to 2026-09-25) | **CLOSED by V1-4e** (158 new games created; watermark advanced; forward season loaded through 2026-09-25). Verified by V1-4e §Hosted proof (a). | V1-4e | n/a — closed |
| GAP-3 | Seed + stats backfills are one-shots — evidence windows drift stale as the season runs | V1-4e report §"Stats + closing-line gap (recovered 07-13..07-15 games)" (line 91-93) and §Executive summary bullet on 2026-07-13..07-15 recovered games | **OPEN.** No `historical_line_results` or `player_game_stats` exist for the recovered 2026-07-13..07-15 games. As the season progresses the drift compounds; seed and stats backfills need periodic top-up. Ticket-forbidden ingestion in V1-4e. **V1-OP-5 STEP 0 update (2026-07-31, `reports/V1_TICKET_OP_5_STEP0_REPORT.md`):** hosted read shows the gap is now 41/43 in-window games with no `player_game_stats` (window 2026-07-12→now), plus 31 past-tip games stuck in `scheduled`. Split into **V1-OP-5a** (BDL stats + finalization, 0 credits, reaches 43/43 games) and **V1-OP-5b** (historical line results, Odds API, ~76 credits for the 19 mapped games, probe-gated). **Blocking coherence finding:** the evidence engine anchors threshold-window observations to `historical_line_results` (`readModelInputBuilder.ts` → `readHistoricalGamesForPlayerMarket`, `FROM historical_line_results`), NOT `player_game_stats`. Restoring pgs alone (5a) lifts the pgs-based V1-OP-4 ingestion gate WITHOUT restoring window coherence — the engine would serve pre-gap windows as current (confidently-wrong). Therefore the correctness half of GAP-3 requires `historical_line_results` (5b / Odds API closing snapshots), and 5a **must not** lift suppression before hlr coherence exists. V1-OP-5a standalone is HELD pending owner re-ruling (options A/B/C in the STEP 0 report). | **V1-OP-5a** (pgs + finalization; prerequisite) + **V1-OP-5b** (historical line results; required for window coherence) | **Yes for correctness.** Missing recent-game stats bias evidence windows for players in those games — and per the STEP 0 anchor finding, a pgs-only restore that lifts suppression makes the bias *served* rather than suppressed. Must land coherently before launch. |
| GAP-4 | No sanctioned `event_reconciliation_queue` drain writer | V1-4e report §STEP 1(b) (line 27-34: "NO drain writer exists"); §STEP 3 queue-row hygiene note (line 113); §Hosted proof (b) (line 320) | **OPEN.** All 5 V1-4e-target queue rows remain `resolution='open'` even after their `provider_games` were approved. `persistSeedEventResolution` never updates the queue. `grep "UPDATE event_reconciliation_queue" src/` returns nothing; `grep "resolution\s*=\s*'resolved'" src/` returns nothing. | UNASSIGNED | **No.** Cosmetic (queue rows leaking as `open` after resolution) — does not affect grain visibility or profile output. |
| GAP-5 | No sanctioned back-link path for `market_snapshots.linked_internal_game_id` | V1-4e report §STEP 1(a) (line 24-25); §STEP 4 (line 117); §Hosted proof (c) (line 325) | **OPEN.** Column is populated only at INSERT by `persistOddsapiSnapshot` from caller-supplied input; no committed code path updates it after approval. 84 V1-4d snapshots remain NULL-linked; V1-4e's bounded re-poll wrote fresh linked snapshots instead. | UNASSIGNED | **No.** V1-4e re-poll pattern replaces the need; but the durable fix (backfill on approval) is desirable when a poll is expensive. |
| GAP-6 | `game_status_observations` is empty; status transitions have no sanctioned owner | V1-4e report §"Status-pipeline conflict I was asked to flag" (line 86-89) | **OPEN.** V1-4b and V1-4e both write `games.status` directly (INSERT/UPDATE only, no observations row). BDL post-final updates do not drive `games.status`. Systemic gap flagged by V1-4e and belongs to a separate ticket. | UNASSIGNED | **No** for now — V1 launch does not depend on status transition auditability, but this must be addressed before any surface reads `game_status_observations` for change history. |
| GAP-7 | Two freshness classifiers under one enum measure different clocks and different terminal states | V1-4e freshness review §Q1-Q2 (this doc: `docs/product/reports/V1_TICKET_4E_FRESHNESS_REVIEW.md`); confirmed by V1-4f report §Executive summary and §"Why two classifiers" | **OPEN.** `src/odds/freshness.ts` measures `now − provider_last_update` (600/1800, terminal `stale`); `src/computation/freshness.ts` measures `now − MAX(observed_at)` (90/300/900, terminal `unavailable`). Same `FreshnessState` enum. The engine consumes only the second via `CurrentMarketRow`. Owner ruled 2026-07-16 that these are two distinct metrics, not conflicting versions of one metric. | **assigned to tickets D and E** (per owner ruling 2026-07-16) | **Yes.** Any change to what the engine consumes requires a new `method_version` per DR-24 + regression fixtures per A1 §12; must land before launch. |
| GAP-8 | §C.3 and §15.2 conflict; §15.2 "wins in code," making §C.3's stale-cap branch structurally unreachable | V1-4e freshness review §Q6-Q7 (line 91-127: "structurally unreachable in the current read-model pipeline"); V1-4f report §Tensions #2 (line 266) | **OPEN.** Composer's `isFreshEnoughForConsensus` (`currentMarketRow.ts:60-63`) empties the offering set for state ∉ {fresh, aging}; every stale grain therefore arrives at the engine with `book_count = 0`; `evaluateC3Freshness('stale', 0)` → `no_current_market_unavailable`, never `stale_current_market_cap`. **Owner ruled: §C.3 wins for evidence consumption**; the code needs to be brought into line. | **assigned to ticket D** (per owner ruling 2026-07-16) | **Yes.** Directly changes what profiles the engine emits; blocks launch until resolved. |
| GAP-9 | Poll wall-clock (299 s) vs a 90 s fresh window; requests are sequential | V1-4f report §Executive summary (line 11) + §Measurement B1 (line 62) + §Tensions #1 (line 265) | **OPEN.** Under sequential per-event polling of 5 events × 4-5 books × 4 markets, poll persist takes ~5 min. The composer's 90 s `FRESH` window cannot be entered by the majority of grains; only the LAST event's grains are ever fresh at engine time. Direct architectural interaction of poll cadence + serialization + threshold magnitude. | **assigned to ticket B** (polling optimization) | **Yes for the operational product.** Even if GAP-7/GAP-8 rulings loosen the composer window, the ~5 min poll dominates cadence choice. |
| GAP-10 | §I.3 assigns the first-profile record to "the V1-A1-3 ticket report"; V1-4e actually discharged it | V1-4e report §"Assigned-report caveat" (line 304); mirror caveat in the freshness review | **OPEN.** Documentation-only. §I.3's wording should be aligned to "the ticket that persists the first operative profile" rather than naming V1-A1-3 specifically. The record's SUBSTANCE (five §I.3 fields) is complete on V1-4e. | UNASSIGNED — documentation alignment | **No.** Substance intact. |
| GAP-11 | Five compact cap tags live in BOTH authority §D.4 and `compose.ts:cap_summary_short` with nothing asserting they match | Cross-file: `docs/product/EVIDENCE_PROFILE_METHOD_V1.md` §D.4 (line 464, "Ratified compact cap tags (OWNER-RATIFIED 2026-07-16, v1.3)"); `src/explanation/compose.ts:149` writes `cap_summary_short`; `tests/explanation/authorityConformance.test.ts` exists but does not currently assert the pair | **OPEN.** No test asserts the code's five tag strings match the authority's five ratified tags. A silent drift is possible on any future edit of either side. Extend `tests/explanation/authorityConformance.test.ts` to enforce the pair. | UNASSIGNED — extend the existing authority-conformance test | **No.** Regression risk only; no current divergence. |
| GAP-12 | v2 stale-present branch may be unreachable in production via the v1 composer offering-set gate | V1-A2-4 report (closing note), 2026-07-24 | **OPEN — under governor review; blocks V1-A2-3 live validation.** The read-model composer empties the offering set for grains outside v1's fresh/aging window (`src/computation/currentMarketRow.ts`, gate around lines 60-63, using `src/computation/freshness.ts` thresholds). Because the v2 populator consumes a `CurrentMarketRow` produced by that composer, a grain older than the v1 gate arrives at the v2 engine with `eligible_book_count = 0` and is classified `absent` (Unavailable + NO_CURRENT_MARKET) rather than reaching v2's `stale-present` branch (Moderate cap + STALE_CURRENT_MARKET). This is the same structural unreachability that motivated evidence_method_v2, relocated upstream of the engine. V1-A2-2's REVISE fixed the ENGINE's freshness verdict; the COMPOSER gate is a different point in the pipeline and was not addressed (`currentMarketRowV2.ts` was deleted during the REVISE under repair 8 as having zero callers — true at the time, because no production wiring existed). Closely related to GAP-8 (same composer gate, v1 side). **RESOLVED by V1-A2-5 (2026-07-24)** — the composer was split into a freshness-neutral assembly core (`src/computation/currentMarketRowCore.ts`), a v1 wrapper retaining the wall-clock gate, and a v2 wrapper (`src/evidence/v2/freshnessNeutralMarketRow.ts`) applying no gate and no clock. A grain older than v1's ~300s window now remains structurally market-present for v2; disposition is decided solely by `classification_age` against the D-A1 thresholds. | V1-A2-5 | **No** (was: yes). |
| GAP-13 | V1-6a preview deployment pending Vercel provisioning | V1_TICKET_6A_REPORT.md Scope I, 2026-07-24 | **OPEN — pending external provisioning by the founder.** Scopes A-H of V1-6a were validated locally. Scope I (Vercel PREVIEW deployment) could not run — no Vercel project or credentials were available to the agent, and creating them is a founder action. Consequence: the amended V1-6 design review has no deployed artifact to review, and per founder amendment 11 a successful preview deploy additionally requires RE-RUNNING the serialization audit against the DEPLOYED response, since local results are not evidence for the deployed artifact. **RESOLVED by V1-6c (2026-07-25).** Preview deployed from the repo root (Root Directory `apps/web`) at `https://web-d6bk2hzqa-bens-projects-593972b9.vercel.app`; the committed `deployedResponse.test.ts` passed 2/2 against the LIVE preview (no prohibited values / secrets in the raw HTML incl. `<script>`; `__next_f` flight marker present; positive control = the approved EMPTY STATE, hosted holding zero v2 profiles; client bundles clean). Required a scoped fix (`outputFileTracingRoot` → repo root, V1-6c) and two founder settings fixes (Output Directory → Next.js default; Deployment Protection off for previews). | V1-6c | **No** (design review now has a deployed artifact). |
| GAP-14 | Board service imports the shared library by deep relative path | V1-6a governor review, 2026-07-24 | **OPEN — cosmetic/maintainability.** `apps/web`'s board service imports the committed DR-20 comparator from the root library via a five-level relative traversal (`../../../../../src/evidence/classification.js`). The dependency direction is CORRECT — the app should consume the shared library rather than duplicate a comparator — but the path is brittle to any future directory move on either side. Resolve with a path alias in `apps/web`'s tsconfig the next time that file is legitimately opened. Not a defect; no behaviour is at risk today. | UNASSIGNED | **No.** |
| GAP-15 | apps/web is pinned to the deprecated-path webpack builder | first Vercel deployment attempt, 2026-07-25 (build log: Turbopack default warning, then WorkerError) | **OPEN — deliberate deferral.** Next 16 defaults to Turbopack. `apps/web` pins `--webpack` because (a) `next.config.mjs`'s extensionAlias (`.js`->`.ts`) is the mechanism by which the app consumes committed backend modules (dr20Compare, the compact renderer) without duplicating them, and (b) the committed serialization audit's bundle scan must run against the same builder that produces the deployed artifact. Migration to Turbopack requires: an equivalent module-resolution mechanism verified under Turbopack; every `next build` invocation switched together; and the FULL serialization audit re-run under the new builder as the acceptance standard. Note: webpack support in Next.js is on a deprecation path; this pin has a shelf life measured in major versions, which is why the gap exists rather than the pin being permanent. | UNASSIGNED (future ticket) | **No.** |
| GAP-16 | Board does not apply the v2 serving gate (§5 display-age suppression) | V1-A2-3 commit-authorization review, 2026-07-26 | **OPEN — MUST be resolved before any real user traffic.** The §5 serving gate is implemented (`src/evidence/v2/servingGate.ts` `evaluateV2ServingGate`: `display_age = serve_now − line_observed_at`; suppress when `> T_SERVE_SUPPRESS_MAX_SECONDS` = 3600), but `apps/web` never calls it. The Board's server query (`boardRepository.ts` `buildBoardQuery`) filters ONLY by `method_version` — no serve-time age filter — and `getBoardData` only ranks + projects; the Board projection does not carry `line_observed_at`. Consequence: the Board serves persisted v2 rows REGARDLESS of display age, so after the V1-A2-3 poll the 99 fresh profiles keep rendering past `display_age > 3600s` instead of the approved empty state returning ~1h later. §5 forbids silent stale serves. Fix (a future surface ticket): the Board read path must compute `display_age` from `line_observed_at` at serve time and suppress rows past the 3600s horizon (no second threshold — D-A1). Do NOT introduce a second suppression threshold. **RESOLVED by V1-6d (2026-07-26)** — `getBoardData` (`apps/web/src/lib/server/boardService.ts`) now captures ONE `serve_now` per request and applies the committed `evaluateV2ServingGate` to every candidate before ranking/projection; rows with `display_age > 3600s` (and null-`line_observed_at` rows) are dropped. The Board query (`buildBoardQuery`) derives `line_observed_at` as the freshest `market_snapshots.observed_at` over the grain's `current_poll` + `self_observed` offerings, BOUNDED by the profile's own `evaluation_reference_time` (`AND ms.observed_at <= ep.evaluation_reference_time`, V1-6d REVISE) so a newer successful poll whose populate did not produce a fresh profile cannot rejuvenate an older profile at serve time; it enters the internal ranked candidate only and is a FORBIDDEN projection key (`boardProjection.ts`) — no timestamp crosses to the browser. When every row is suppressed the page renders the approved empty state (identical to zero rows). `servingGate.ts`, `thresholds.ts`, and every `src/evidence` module are unchanged (compose-only); no second threshold introduced. Verified by `boardServingGate.test.ts` (boundary 3599/3600 serve, 3601 suppress; one-serve_now batch semantics; mixed→survivors-ordered; all-aged→empty; read-only non-mutation; forbidden-key rejection) and the full serialization audit re-run green. Visible age UI (an "updated N min ago" marker) is DEFERRED to design review, not a gap. | V1-6d | **No today** (Board not yet on real user traffic); **blocks real user traffic**. |
| GAP-17 | compact renderer accepts semantically incoherent cap combinations | V1-6e fixture construction, 2026-07-27 | **OPEN — cosmetic/type-tightening.** Quality-cap tags on Unavailable/Insufficient/Mixed profiles are type-constructible (the compact renderer, `src/explanation/compose.ts` `renderCompactExplanation`, has no cap-consistency guard) but semantically incoherent — a cap presupposes a classified score to downgrade. No production path can produce the combination (the engine caps only classified results), so this is latent type looseness, not a live defect. Resolution direction: a discriminated type or a renderer guard rejecting cap tags on non-classified profiles, next time the renderer is legitimately opened. | UNASSIGNED | **No.** |
| GAP-18 | Evidence Strip positional completeness is not yet achievable | V1-8a0 STEP 0 (2026-07-28) | **OPEN — blocks full Board Strip rendering.** Grammar §2.2 requires the Evidence Strip to represent the chronological REQUESTED window with DNP/ineligible positions visible ("holds position, carries no verdict"; a chronological omission location is itself evidence — "their absence is information"). The positional metadata that makes this possible — **opponent identity, home/away, and DNP/ineligible state** — is NOT obtainable in `src/` at population time: the committed historical reader (`src/evidence/driver/readModelInputBuilder.ts readHistoricalGamesForPlayerMarket`) returns only `{game_date_utc, player_stat_value, is_backfilled_historical}`; no `src/` read path reads `player_game_stats.internal_opponent_team_id`/`is_home`/`minutes_status`; and DNP/ineligible games are structurally absent from `historical_line_results`. The reader that supplies this (V1-7b `readSeries`) lives app-side (`apps/web/src/lib/server/researchRepository.ts`), not `src/`. V1-8a0 persists the window AGGREGATES and the source-identity set (both at the writer boundary) but explicitly does NOT build the per-game series. **The Grammar is unchanged and was not reinterpreted.** Assigned: **V1-8a0a** (per-game threshold-relative series exposure) and **V1-8a0b** (shared `src`-owned historical-series reader ownership). | V1-8a0a / V1-8a0b | **No today** (Board Strip not yet built); **blocks full Board Strip rendering**. |
| GAP-19 | writer accepts evidence inputs and classification output as independent parameters | V1-8a0 governor review, 2026-07-28 | **OPEN — architecture.** `writeV2EvidenceProfile` (`src/evidence/v2/writerV2.ts:64`) takes `input: EvidenceProfileInput` and `result: V2ClassifiedResult` as two independent parameters. Because the inputs and the classification output arrive separately, the invariant that they describe the SAME evaluation event is enforced procedurally (by the caller passing the matching pair) and by test, NOT by construction (the type system cannot witness that a given `input` produced a given `result`). Resolution direction: **V1-ARCH-2 — Unified Classified Evaluation Object**, a single object carrying the inputs and their classification together so the writer cannot be handed a mismatched pair. | V1-ARCH-2 (not scheduled; NOT a Board prerequisite) | **No.** |
| GAP-20 | shared historical-series reader has no committed test executing its SQL | V1-GOV-6, 2026-07-28 | **OPEN — test coverage.** `src/computation/historicalSeriesRead.ts` (`readHistoricalSeries`, promoted in V1-8a0b) has no committed test that runs its SQL against a database. Committed coverage is of the CONSUMER contract only — the `ResearchSeriesRow` shape and the projection/window computation over it — via the in-memory `FixtureResearchRepository` in `apps/web/test/researchProjection.test.ts` and `researchView.test.ts`; the sole `PostgresResearchRepository` touch asserts only that `queryResearchGrain` is a function ("no connection made"). The SQL-execution path was validated by an ad-hoc 7-grain old-vs-new comparison during V1-8a0b (necessarily ad-hoc — the old path was deleted by that ticket). This posture PREDATES V1-8a0b (the app-side `readSeries` never had committed DB coverage either) and is UNCHANGED by it. Resolution direction: **V1-8a0a** is the natural place to close it — it exercises the reader against a database to persist the series, so a committed integration test can assert the reader's rows there. **CLOSED by V1-8a0a (2026-07-28)** — `tests/integration/v1_8a0a_series.integration.test.ts` invokes the REAL `readHistoricalSeries` against local Docker and asserts its frozen contract (ordering oldest→newest, nullability of DNP/ineligible `stat_value`, eligibility semantics, provenance/backfilled flag, DNP/ineligible positions present holding chronological place, and the stable `internal_game_id` row identity). Copying the SQL into a test was NOT used; the test executes the shared reader itself. | V1-8a0a | **No.** |
| GAP-21 | Board information band crosses the RSC boundary as a large client payload | V1-8a1 governor review, 2026-07-29 | **OPEN — performance.** The band renders in the `'use client'` `BoardTable`, so the full season series crosses the RSC flight — roughly 23 positions per row, ~1,150 position objects for a 50-row Board, on mobile. This is the shape V1-8a1 was asked for and justified (SZN is a full Strip per Parity §1.3 #29, and L5/L10/L20 are tail sub-spans of the one series, so no duplication), but it is a real performance question for a surface whose value is feeling fast. Resolution direction: V1-8a2 evaluates whether Strips can render server-side, or whether the SZN span needs a narrower carry on a dense board. Do NOT narrow it here — narrowing without evaluation would fail to reproduce the SZN Strip. | V1-8a2 | **No.** |
| V1-INC-1 | Producer deployed before hosted evidence-input schema | 2026-07-29 | **CLOSED — incident record.** V1-8a0's commit pushed the writer that INSERTs into `evidence_profile_window_aggregates` while the migration was deliberately deferred from hosted; scheduled cycles from that push until the schema landed (~00:5x UTC) failed with `relation ... does not exist`. Observed at least one failed cycle (23:30 UTC) that spent polling credits before failing at populate; no data corruption (transaction rollback). Root cause: the deferral was reasoned in isolation without checking it against a producer already running on a schedule. Corrective rule now applied: schema precedes producer, enforced in V1-8a0a Scope E. | V1-8a0a (corrective rule) | **No.** |
| V1-OPS-3 | Scheduled poll failure signaling | 2026-07-29 | **OPEN — operations.** A failed cycle records `outcome` and `error_summary` in `poll_cycles` and appears in the Actions log, and **nothing surfaces it** — the V1-INC-1 failure ran unnoticed for roughly two hours and was found only because an observation gate happened to look. The ops loop has no failure signal. Resolution direction: a lightweight alert on consecutive non-`completed` cycles or on any `failed` outcome. | UNASSIGNED (operations) | **No** (but it is the difference between a silent failure and a known one). |
| GAP-22 | Board row lacks matchup and tipoff | V1-8a2 governor review, 2026-07-29 | **OPEN — parity.** Parity Spec §1.3 #21 lists matchup and tipoff as DIRECT row content, but the V1-8a1 projection contract does not carry them — a governor omission: the contract's field list was built from the original V1-8a1 inventory and never reconciled against the spec's full row anatomy. Both values are available at the Board read path (`games.scheduled_start_utc`; home/away team identity) and are merely unprojected. V1-8a2 correctly rendered the projected identity without fabricating them. Resolution direction: one small follow-up adding both fields to the projection AND their rendering together — neither is meaningful alone, and coupling them avoids two tickets for one row change. | follow-up (unnumbered; governor to issue) | **No.** |
| V1-7d | Research View per-game chart axis repair | V1-8a3, 2026-07-30 | **OPEN — rendering.** The Research View per-game chart has overlapping opponent labels, an unreadable x-axis, ghost/eligible annotation collisions, and excessive mobile chart-label density. This is a **rendering defect only — no computation change and no evidence-semantic change** — and it must NOT expand the Board ticket. (No broader Research View revision pass is currently registered, so this is recorded standalone; if one is later opened, record V1-7d as a blocking sub-ticket under it rather than duplicating authority.) **CLOSED by V1-8b (`a450ffd`, 2026-07-31).** The R4 chart rebuild (`apps/web/components/research/EvidenceChart.tsx`) fixes each named defect, evidence per defect: (a) *overlapping opponent labels* — opponent labels REMOVED from the plot (the `researchView.test.ts` V1-8b chart test asserts `!CHART.includes('opponent_label')`); opponents now render only in the `GameHistory` rows. (b) *unreadable x-axis* — ONE short date label per fixed-34px column (`shortDate`), no per-bar prose. (c) *ghost/eligible annotation collisions* — the in-plot `DNP`/`inelig.` `<text>` annotations were removed; ineligible/DNP positions are dashed ghost bars carrying `data-kind` and holding chronological place. (d) *excessive mobile label density* — fixed-width columns + `overflowX:'auto'` horizontal scroll for L20/Season instead of compressing labels. Verified by the V1-8b serialization audit (20/20 against real rendered HTML) AND the founder's 390px device review (explicitly confirmed L20/Season intentional horizontal scrolling and DNP/ineligible chronological placement). | V1-8b | **No.** |
| GAP-23 | Internal game identity exposed in Research route | V1-8b governor review, 2026-07-30 | **OPEN — architecture / containment (governance).** The Research route is addressed by the raw internal game identity: the route folder is `apps/web/app/research/[internal_game_id]/[internal_player_id]/[market_key]/page.tsx`, and the Board builds that href from `internal_game_id` (`apps/web/src/lib/server/boardService.ts` `research_href`), so `internal_game_id` (and `internal_player_id`) sit in the **browser URL**. This PREDATES V1-8b (the route naming is V1-7a; the href is V1-8a2) and was **NOT introduced by V1-8b** — V1-8b touched no route and no `boardService`, and its projection/response contract remains clean (Amendment 21's forbidden-key set and browser-visible RESPONSE body carry no internal id, verified). It **does not block the V1-8b visual review.** It may conflict with the SPIRIT of Amendment 21 (Amendment 21's text targets the projection key-set and the browser-visible response; the URL addressing handle is a distinct, previously-unreasoned surface). Resolution direction: consider an opaque public route identifier or another non-internal routing key (e.g. a per-grain slug / signed handle resolved server-side) so no internal id appears in the address bar. **No route change is authorized in V1-8b**; this is recorded standalone for a future ticket. | UNASSIGNED (future ticket) | **No** (does not block V1-8b). |
| GAP-24 | No preview route renders the current V1-8a3 Board surface | V1-GOV-7 governance pass, 2026-07-31 | **OPEN — preview coverage.** The current Board surface (`apps/web/components/board/BoardSurface.tsx`, V1-8a2/8a3 — `BoardChrome`/`BoardRow`/`BoardDisclosure`/`LockedContinuation`) is rendered ONLY by the production `/board` route. The `/design-preview*` routes render OLDER variants: `/design-preview` → the legacy `'use client'` `components/BoardTable.tsx` (V1-6a/6e); `/design-preview/a` and `/b` → `components/preview/DesignVariantA`/`DesignVariantB` (design-exploration variants). The current surface renders with fixtures only when the server is booted with the non-public env var `BOARD_DATA_SOURCE=fixture` (`boardService.ts` repository selection) and `/board` is fetched — which is exactly how `test-audit/serialization.test.ts` exercises it, NOT a URL-reachable preview. Consequence: there is no `/design-preview` equivalent for device review of the current Board (unlike Research View's `/design-preview/research/[idx]`); a device preview requires a deploy with `BOARD_DATA_SOURCE=fixture` pointed at `/board`. Not a correctness defect and no browser leak — a preview-tooling gap. Resolution direction: add a fixture-backed design-preview route that renders the current `BoardSurface` (mirroring the research preview), or formalize the `BOARD_DATA_SOURCE=fixture` deploy convention for Board device review. Discovered while answering a founder question in the V1-GOV-7 pass; predates and is unrelated to V1-8b. | UNASSIGNED (future ticket) | **No.** |
| GAP-25 | Ingestion gate's fail-safe suppression is indistinguishable from a real lag suppression | V1-OP-4 governor verification, 2026-07-31 | **OPEN — operations.** The pure gate `decideIngestionCurrency` (`src/ops/ingestionGate.ts`) suppresses the whole Board when `oldest_unresolved_tip` is unparseable on a live source — this is **correct, fail-safe** behaviour (honest-empty beats serving on an uncomputable lag; owner ruling 2026-07-31, consistent with the standing honest-empty ruling). **But** the serve path (`getBoardData` → `buildIngestionServeLogLine`) then emits the **standard** `BOARD_SUPPRESSED ingestion_behind: …` log, whose three numbers are meaningless/absent for a malformed value. An operator therefore sees a dark Board attributed to ingestion lag when the actual cause is a malformed timestamp — different failure, different fix, same message. The decision itself is correct and is NOT to change. Resolution direction: emit a **distinct** suppression reason on the log (a separate prefix, e.g. `BOARD_SUPPRESSED ingestion_unparseable`, or a reason field) so a malformed value is greppable separately from genuine lag. Discovered during the V1-OP-4 governor verification (the implementation agent terminated before self-reporting; verified by governor inspection — see `reports/V1_TICKET_OP_4_REPORT.md` §5). Did not hold the V1-OP-4 commit. | V1-OP-4b (same serving path) | **No.** |

---

## Traceability index

Every row above cites a specific report + section. Reports live in `docs/product/reports/`:

- `V1_TICKET_4C_REPORT.md` — Phase A historical-line-results survey (GAP-1)
- `V1_TICKET_4D_REPORT.md` — first live current_poll (GAP-2 origin)
- `V1_TICKET_4E_REPORT.md` — forward game ingestion; back-link + queue-drain audits (GAP-2 close, GAP-3, GAP-4, GAP-5, GAP-6, GAP-10)
- `V1_TICKET_4E_FRESHNESS_REVIEW.md` — targeted governor review answering 12 questions (GAP-7, GAP-8)
- `V1_TICKET_4F_REPORT.md` — freshness decay & book movement probe (GAP-9)
- `EVIDENCE_PROFILE_METHOD_V1.md` §D.4 + `src/explanation/compose.ts` — cap-tag pair (GAP-11)
- `V1_TICKET_A2_4_REPORT.md` — v2 read-model input builder; composer-gate closing note (GAP-12)
- `V1_TICKET_A2_5_REPORT.md` — freshness-neutral composer core + v1/v2 wrappers (GAP-12 resolution)
- `V1_TICKET_6A_REPORT.md` — Board vertical slice; server-only boundary + committed serialization audit (GAP-13, GAP-14)
- `V1_TICKET_6B_REPORT.md` — builder parity (webpack pin) + preview deployment (GAP-13 close, GAP-15)
- `V1_TICKET_6C_REPORT.md` — preview deploy + deployed-response audit against the live URL (GAP-13 resolution)
- `V1_TICKET_A2_3_REPORT.md` — v2 live validation (99 profiles); appendix records the Board serving-gate gap (GAP-16)
- `V1_TICKET_6D_REPORT.md` — Board serving gate wired at serve time; §5 display-age suppression (GAP-16 resolution)
- `V1_TICKET_6E_REPORT.md` — fixture design-preview route (23-profile matrix); renderer cap-looseness finding (GAP-17)

## Register maintenance

- A gap moves to **CLOSED** only when a ticket report demonstrates the fix + verification.
- A gap moves to **DEFERRED** only by explicit owner ruling with the ruling cited.
- Do NOT delete rows — closed rows remain here as governance history.
- "Blocks launch?" is a binary factual read against the current V1 launch definition; adjust when the launch definition changes, cite the change.
